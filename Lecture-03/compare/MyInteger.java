// The MyInteger class is used to store an integer inside an object.
public class MyInteger {
    int value; // Field to store the integer value.

    // Constructor to initialize the object with a value.
    public MyInteger(int value) {
        this.value = value;
    }
}
