// Animal.java
// This is the parent class representing a generic animal.
// It contains a single method 'speak', which is overridden in child classes.

public class Animal {
    // Generic method to output a sound (to be overridden in subclasses)
    public void speak() {
        System.out.println("Some generic animal sound.");
    }
}
